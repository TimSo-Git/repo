using System;

class Program{
    static void Main(){
        double pi = Math.PI;
        double radius = 2.50;
        double area = pi * (radius * radius);

        Console.WriteLine($"The area of the circle is {area}");
    }
}